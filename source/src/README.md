# SRC - Código Fonte 
Código fonte do ForntEnd feito em reactjs
```shell
├── Components // Contém todos os componentes usados pelas telas (pages)
├── README.md
├── Resources // Contém todos os recursos externos que o sistema consome (API GraphlQL)
│   └── GQLClient.tsx
└── Theme // Contém o tema usado pelo cliente  (usa o tema do Material UI)
    └── index.tsx

```
